instructions:
-install node packages (npm install in /roll folder)
-generate the .csv with the DV volcano.ipynb in /roll/data (requires pandas, numpy, seaborn and matplotlib)
-run node server.js in /roll
-go to localhost:8082/index on browser

note that the speed up and speed down buttons don't do anything yet.